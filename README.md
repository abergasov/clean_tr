### remove tracking list from torrent
```bash
# install binary
sudo make build
```

zsh usage 
```bash
# remove tracking list from all torrents at specified directory
torrent_cleaner --path /home/user/Downloads
```